
server:
  port: 8081

spring:
  datasource:
    name: ideatest
    url: jdbc:mysql://localhost:3306/ideatest?useSSL=false&serverTimezone=UTC
    username: java
    password: java
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.test.bean